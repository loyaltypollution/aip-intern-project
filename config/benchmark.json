{
  "benchmark": {
    "n_runs_per_task": 20,
    "systems": ["single", "mesh"],
    "output_dir": "artifacts/benchmarks"
  },
  "llm": {
    "model": "gemini-2.5-flash",
    "base_url": "${OPENAI_BASE_URL}",
    "api_key": "${OPENAI_API_KEY}",
    "temperature": 0.0,
    "max_tokens": 4096,
    "request_timeout": 120
  },
  "mcp": {
    "workspace_root": "."
  },
  "artifacts": {
    "output_dir": "artifacts/"
  },
  "tasks": {
    "simple": "Summarise citizen feedback and extract structured fields (id, category, urgency, owner, summary, pii_flagged) with minimal reasoning.",
    "complex": "Perform a multi-step workflow: parse all feedback, cross-reference policy snippets, prioritise urgent issues, generate a justification-rich action brief, and produce category-specific response templates with explicit policy alignment."
  }
}