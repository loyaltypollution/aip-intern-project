# Handover — Agent vs Mesh Benchmark Integration

## Date
2026-04-22

## Scope Completed
This session focused on integrating and validating a **benchmark-to-decision pipeline** to compare:

- **Single-agent baseline** (LangGraph)
- **Agentic mesh** (LangGraph orchestrator + CrewAI roles + tools)

The goal was to produce repeatable benchmark outputs and a decision-oriented artifact under realistic constraints.

---

## What Was Integrated

## 1) Benchmark orchestration layer
Added/used a thin benchmark entrypoint:

- `scripts/run_benchmark.py`

Responsibilities:
- run baseline and mesh using existing runners
- use shared task definitions/config
- collect per-run metrics
- aggregate results
- emit decision-ready outputs (CSV/JSONL/MD)

## 2) Existing runners reused (no re-architecture)
Used existing:
- `scripts/run_baseline.py`
- `scripts/run_mesh.py`
- `src/aip_intern/baseline/runner.py`
- `src/aip_intern/mesh/runner.py`

## 3) Analysis integration
Used/extended analysis layer:
- `analysis/compare.py`
- `analysis/plots.py`
- notebook handoff: `notebooks/phase2_mesh/03_decision_pipeline.ipynb`

## 4) Config/env alignment for Gemini
Environment/config path supports Gemini-only setup (OpenAI-compatible endpoint path resolution was aligned in prior updates).

Expected env:
- `GEMINI_API_KEY`
- `GEMINI_MODEL` (example: `gemini-2.5-flash`)
- optional: `GEMINI_BASE_URL`

---

## Key Fixes During Execution

## 1) Import path issues
- `aip_intern` import errors were resolved by ensuring `src/` is on `sys.path` in scripts/tests bootstrap.

## 2) Mesh provider error (CrewAI/litellm)
Error seen:
- `LLM Provider NOT provided ... model=Qwen/...`

Resolution:
- install Gemini provider for CrewAI:
  - `pip install "crewai[google-genai]"`
- use a Gemini model name in config for mesh runs.

## 3) Filesystem/tool path mismatch
Error seen repeatedly:
- `data/... is not a directory`
- actual files are under `workspace/data/...`

Resolution:
- align prompts/tasks/tool paths with configured workspace root.
- effective paths should resolve to:
  - `workspace/data/feedback/...`
  - `workspace/data/policy_snippets.md`

## 4) Baseline graph/node progression issues
Observed issues included:
- response node requiring prior brief output
- path expectations around triage artifact names

Status:
- baseline dry-run eventually reached successful completion after node/prompt/path corrections.

---

## Benchmark Output Locations

Primary outputs:
- `artifacts/benchmarks/benchmark_records.jsonl`
- `artifacts/benchmarks/benchmark_records.csv`
- `artifacts/benchmarks/benchmark_summary.csv`
- `artifacts/benchmarks/decision_framework.md` (if generated by current script version)

Per-run artifacts:
- `artifacts/<run_id>/metrics.json`
- `artifacts/<run_id>/outputs/*`

---

## Current Benchmark Snapshot (from `benchmark_summary.csv`)

```
mesh,complex: success=1.0, latency~59.2s, tokens=0
mesh,simple:  success=1.0, latency~59.6s, tokens=0
single,complex: success=1.0, latency~33.0s, tokens=46274
single,simple:  success=0.0, latency~12.1s, tokens=0
```

Important caveats:
- this snapshot reflects **1 run per cell** (`runs=1`), not final experimental significance
- mesh token accounting currently appears as zero and needs metric wiring verification for fair cost comparison
- single simple task had a failure in this snapshot

---

## Known Gaps Before Final Report

1. **Run count not at target**  
   Need full planned runs (e.g., 20/task/system).

2. **Token accounting parity**  
   Mesh currently reports zeros; verify capture from CrewAI/litellm responses.

3. **Task/path consistency lock**  
   Ensure baseline and mesh both use identical task inputs and path references.

4. **Decision artifact quality**  
   Re-generate decision matrix after full runs.

---

## Recommended Next Steps (Execution Order)

1. Validate both dry-runs:
   - `python scripts/run_baseline.py --config config/baseline.yaml --dry-run`
   - `python scripts/run_mesh.py --config config/mesh.yaml --dry-run`

2. Run full sweeps:
   - `python scripts/run_baseline.py --config config/baseline.yaml`
   - `python scripts/run_mesh.py --config config/mesh.yaml`

3. Run benchmark aggregation:
   - `python scripts/run_benchmark.py --config config/benchmark.json`

4. Generate decision artifacts/plots:
   - via `analysis/compare.py`, `analysis/plots.py`
   - and `notebooks/phase2_mesh/03_decision_pipeline.ipynb`

5. Publish final decision framework:
   - where mesh wins (complex/tool-heavy)
   - where single wins (latency/reliability/simple workflows)
   - GCC-specific tradeoff statement

---

## Operational Notes

- Long benchmark runs are expected; mesh orchestration is expensive in wall time.
- To avoid mixed history, use a timestamped artifact subdirectory per benchmark campaign.
- Keep model/temperature/max_tokens/task set identical across systems for valid comparison.